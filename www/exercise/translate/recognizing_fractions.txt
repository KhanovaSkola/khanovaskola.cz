312.1 	 Recognizing fractions

312.2 	 What is the numerator of the fraction <code>\dfrac{<var>NUMERATOR</var>}{<var>DENOMINATOR</var>}</code>?

312.4 	 Fractions help represent parts of a whole.

312.5 	 The fraction <code>\dfrac{<var>NUMERATOR</var>}{<var>DENOMINATOR</var>}</code> could represent <var>NUMERATOR</var> out of <var>DENOMINATOR</var> slices of pie. In other words, the pie has been cut into <var>DENOMINATOR</var> slices, and we are only considering <var>NUMERATOR</var> of those slices.

312.6 	 The numerator is the number of slices we consider, and it is written above the fraction line. The denominator is the total number of slices, and it is written below the line.

312.7 	 Thus, the numerator is <code><var>NUMERATOR</var></code>.

312.8 	 What is the denominator of the fraction <code>\dfrac{<var>NUMERATOR</var>}{<var>DENOMINATOR</var>}</code>?

312.10 	 Thus, the denominator is <code><var>DENOMINATOR</var></code>.

