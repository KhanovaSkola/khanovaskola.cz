84.1 	 Converting decimals to percents

84.2 	 Express the decimal as a percent.

84.5 	 Rewrite the decimal as a fraction with a denominator of 100.

84.8 	 <code><var>PERCENT</var></code> per hundred = <code><var>PERCENT</var></code> per cent = <code><var>PERCENT</var></code> percent

84.9 	 Another way to convert a decimal to a percent is to multiply by <code>100</code> (or move the decimal two places to the right) and add a percent sign.

