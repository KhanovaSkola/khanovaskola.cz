87.1 	 Converting percents to decimals

87.2 	 Express the percent as a decimal.

87.5 	 <code><var>PERCENT</var></code> percent = <code><var>PERCENT</var></code> per cent = <code><var>PERCENT</var></code> per hundred

87.8 	 Another way to convert a percent to a decimal is to divide by <code>100</code> (or move the decimal two places to the left) and remove the percent sign.

