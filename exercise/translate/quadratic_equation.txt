289.1 	 Quadratic formula

289.2 	 Let <code>f(x) = <var>F_TEXT</var></code>.

289.3 	 Where does this function intersect the x-axis (i.e. what are the roots or zeroes of <code>f(x)</code>)?

289.5 	 The function intersects the x-axis when <code>f(x) = 0</code>, so you need to solve the equation:

289.7 	 Use the quadratic formula to solve <code>ax^2 + bx + c = 0</code>:

